<%= form_for [@timesheet, @entry] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.text_field :athlete_name, class: 'form-control', id: 'typeahead', placeholder: 'Search for athlete' %>
  <%= f.submit %>
<% end %>
