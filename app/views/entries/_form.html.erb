<%= form_for [@timesheet, @entry] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.label :athlete_name, 'Athlete' %>
  <%= f.collection_select(:athlete_id, Athlete.all, :id, :name, :prompt => "Athlete") %>
  <%= f.submit %>
<% end %>